/* Minimal DataTables stub to prevent errors */
(function(){
  if (typeof $ !== 'undefined') {
    $.fn.dataTable = {
      Api: function(){},
      ext: { errMode: 'none' }
    };
    $.fn.DataTable = function() { 
      console.log("DataTables not fully loaded");
      return this; 
    };
  }
})(); 